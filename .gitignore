# build artefacts
bin/
dist/
*.test
*.out

# per-run artefacts (HTML, markdown, screenshots, events.log)
runs/

# credential bundles — contain auth tokens, never commit
profiles/

# dev profile dirs used by smoketests
.smokeprofile/

# editor / OS
.DS_Store
.idea/
.vscode/

# Claude Code agent state (per-conversation; never check in)
.claude/

# Sibling project — separate repo, just nested in this dir
greenthread/
